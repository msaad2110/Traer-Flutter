


import 'package:stacked/stacked.dart';

class NewTripDatePickerViewModel extends BaseViewModel{
  var defaultStartDate = DateTime.now();
  var defaultEndDate = DateTime.now().add(const Duration(days: 3));

}